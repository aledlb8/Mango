{
  "name": "@mango/notification-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "@mango/contracts": "workspace:*",
    "@types/web-push": "^3.6.4",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
