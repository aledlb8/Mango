{
  "name": "@mango/identity-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "@mango/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
